---
id: s25t01
slug: support-developerreview-process
status: pending
---

# Support developer-review process

Developer should not mark task as `done` but rather move it `in-review`.
Parent tasks are still `in-progress` then (TBD: only leaf tasks are `in-review`).

Reviewer adds subtasks turning reviewing task back to `pending`.

TBD: it's good to have more detailed statuses like (`need-fixes`, `reopened`, etc.)
