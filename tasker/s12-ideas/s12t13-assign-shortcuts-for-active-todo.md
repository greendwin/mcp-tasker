---
id: s12t13
slug: assign-shortcuts-for-active-todo
status: pending
---

# Assign shortcuts for active todo tasks

Lets assign shortcuts to active todo tasks.
It would be nice for easier referencing them.

We can make this numbering stable (based on current tasks list, just `t1`, `t2` and so on).
We can cap it to 10 references (`t0` is last), so it would no problem with references `t103` -> `(t1)03`.
If there is more than ten marked todo tasks (note, that we have not to select each individual task as todo - only parent stories) - then just don't assign further, or... we can continue with (!) letters (e.g. ta, tb, tc).
TBD: we can *start* from letters, so distinctions would be more clear, `ta03` - its a shortcut anyway.

We can show these ids as shortcut markers (instead of `(todo)` marker).
We can limit amount of todo tasks by (a-z + 0-9) symbols. Need more -- untodo or archive finished.
